{
  "name": "sistema-frontend",
  "version": "1.0.0",
  "scripts": {
  "dev": "vite",
  "build:sistema": "vite build",
  "build:workspace": "vite build --config vite.workspace.config.js",
  "build": "npm run build:sistema && npm run build:workspace",
  "preview": "vite preview"
},
  "devDependencies": {
    "vite": "^8.0.11",
    "vite-plugin-pwa": "^1.3.0"
  }
}
